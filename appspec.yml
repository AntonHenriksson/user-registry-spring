version: 0.0
os: linux
files:
  - source: /docker-compose.yml
    destination: /home/ec2-user/
  - source: /deploy.sh
    destination: /home/ec2-user/
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 300
      runas: root